// JavaScript program to find the length of a string without using built-in methods
let str = "Syed Urooj Ahmed"; // Example string
let length = 0;   // Variable to store the length

// Loop through each character in the string
for (let char of str) {
    length++; // Increment the length counter for each character
}

console.log("The length of the string is:", length);
